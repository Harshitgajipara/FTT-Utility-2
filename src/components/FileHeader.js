class FileHeader {
    constructor(lineNumber, registerType, proposedSalesDate) {
        this.lineNumber = lineNumber;
        this.registerType = registerType;
        this.proposedSalesDate = proposedSalesDate;
    }
}

export default FileHeader;